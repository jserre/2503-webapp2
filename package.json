{
  "name": "innotion-web-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "vercel-build": "npm install -g vite && vite build"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
  }
}